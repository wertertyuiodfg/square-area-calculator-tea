// index.js

function calculateSquareArea(sideLength) {
    return sideLength * sideLength;
  }
  
  const sideLength = 5;
  const area = calculateSquareArea(sideLength);
  console.log(`Area of the square with side length ${sideLength} is: ${area}`);
  